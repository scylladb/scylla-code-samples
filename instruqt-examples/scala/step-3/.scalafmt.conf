version = "3.7.0"
```

```scala:instruqt-examples/scala/step-3/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-3/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-3/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-3/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-3/project/Dependencies.scala
import sbt._

object Dependencies {
  lazy val scalaTest = "org.scalatest" %% "scalatest" % "3.2.0"
  lazy val phantom = "com.outworkers" %% "phantom-dsl" % "2.59.0"
  lazy val scalaReflect = "org.scala-lang" % "scala-reflect" % "2.13.6"
}
```

```scala:instruqt-examples/scala/step-3/project/build.properties
sbt.version=1.9.0
```

```scala:instruqt-examples/scala/step-3/project/plugins.sbt
addSbtPlugin("com.eed3si9n" % "sbt-assembly" % "2.1.0")
```

```scala:instruqt-examples/scala/step-3/.gitignore
target/
.idea/
*.iml
```

```scala:instruqt-examples/scala/step-3/build.sbt
import Dependencies._

ThisBuild / scalaVersion := "2.13.16"
ThisBuild / version := "0.1.0-SNAPSHOT"
ThisBuild / organization := "com.scylla.mms"

// Add Java 21 compatibility settings
ThisBuild / javacOptions ++= Seq("-source", "11", "-target", "11")
ThisBuild / javaOptions ++= Seq("--add-opens=java.base/java.lang=ALL-UNNAMED")

lazy val root = (project in file("."))
  .settings(
    name := "mms-scala-app",
    mainClass := Some("com.scylla.mms.App"),
    libraryDependencies ++= Seq(
      phantom,
      scalaReflect,
      scalaTest % Test
    ),
    assembly / assemblyMergeStrategy := {
      case PathList("META-INF", "io.netty.versions.properties") =>
        MergeStrategy.first
      case x =>
        val oldStrategy = (assembly / assemblyMergeStrategy).value
        oldStrategy(x)
    }
  )
```

```scala:instruqt-examples/scala/step-3/App.scala
package com.scylla.mms

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant
import com.scylla.mms.service.MutantsService

import scala.concurrent.Await
import scala.concurrent.duration._
import scala.util.Success

object App {
  def main(args: Array[String]): Unit = {
    val connection: CassandraConnection = ContactPoints(Seq("localhost"))
      .keySpace("catalog")
    val db = new MutantsDatabase(connection)
    val svc = new MutantsService(db)

    println("Successfully connected to ScyllaDB database!")
    
    Await.result(
      for {
        _ <- svc.getAll().andThen {
          case Success(mutants) =>
            println("Initial Mutants: " + mutants.mkString(", "))
        }
        _ <- svc.insertMutant(
          Mutant(
            "Miles",
            "Morales", 
            "42 Brooklyn St",
            "https://www.facebook.com/miles-morales"
          )
        )
        _ <- svc.getAll().andThen {
          case Success(mutants) =>
            println("Mutants after INSERT: " + mutants.mkString(", "))
          }
      } yield (),
      30.seconds
    )

    db.shutdown()
  }
}
```

```scala:instruqt-examples/scala/step-2/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-2/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-2/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-2/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-2/build.sbt
import Dependencies._

ThisBuild / scalaVersion := "2.13.16"
ThisBuild / version := "0.1.0-SNAPSHOT"
ThisBuild / organization := "com.scylla.mms"

// Add Java 21 compatibility settings
ThisBuild / javacOptions ++= Seq("-source", "11", "-target", "11")
ThisBuild / javaOptions ++= Seq("--add-opens=java.base/java.lang=ALL-UNNAMED")

lazy val root = (project in file("."))
  .settings(
    name := "mms-scala-app",
    mainClass := Some("com.scylla.mms.App"),
    libraryDependencies ++= Seq(
      phantom,
      scalaReflect,
      scalaTest % Test
    ),
    assembly / assemblyMergeStrategy := {
      case PathList("META-INF", "io.netty.versions.properties") =>
        MergeStrategy.first
      case x =>
        val oldStrategy = (assembly / assemblyMergeStrategy).value
        oldStrategy(x)
    }
  )
```

```scala:instruqt-examples/scala/step-2/App.scala
package com.scylla.mms

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant
import com.scylla.mms.service.MutantsService

import scala.concurrent.Await
import scala.concurrent.duration._
import scala.util.Success

object App {
  def main(args: Array[String]): Unit = {
    val connection: CassandraConnection = ContactPoints(Seq("localhost"))
      .keySpace("catalog")
    val db = new MutantsDatabase(connection)
    val svc = new MutantsService(db)

    println("Successfully connected to ScyllaDB database!")
    
    Await.result(
      for {
        _ <- svc.getAll().andThen {
          case Success(mutants) =>
            println("Data that we have in the catalog".center(50, "="))
            mutants.foreach(mutant => 
              println(s"${mutant.firstName} ${mutant.lastName}")
            )
        }
      } yield (),
      30.seconds
    )

    db.shutdown()
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/build.sbt
import Dependencies._

ThisBuild / scalaVersion := "2.13.16"
ThisBuild / version := "0.1.0-SNAPSHOT"
ThisBuild / organization := "com.scylla.mms"

// Add Java 21 compatibility settings
ThisBuild / javacOptions ++= Seq("-source", "11", "-target", "11")
ThisBuild / javaOptions ++= Seq("--add-opens=java.base/java.lang=ALL-UNNAMED")

lazy val root = (project in file("."))
  .settings(
    name := "mms-scala-app",
    mainClass := Some("com.scylla.mms.App"),
    libraryDependencies ++= Seq(
      phantom,
      scalaReflect,
      scalaTest % Test
    ),
    assembly / assemblyMergeStrategy := {
      case PathList("META-INF", "io.netty.versions.properties") =>
        MergeStrategy.first
      case x =>
        val oldStrategy = (assembly / assemblyMergeStrategy).value
        oldStrategy(x)
    }
  )
```

```scala:instruqt-examples/scala/step-1/App.scala
package com.scylla.mms

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.service.MutantsService

import scala.concurrent.Await
import scala.concurrent.duration._

object App {
  def main(args: Array[String]): Unit = {
    val connection: CassandraConnection = ContactPoints(Seq("localhost"))
      .keySpace("catalog")
    val db = new MutantsDatabase(connection)
    val svc = new MutantsService(db)

    println("Successfully connected to ScyllaDB database!")
    
    db.shutdown()
  }
}
```

```scala:instruqt-examples/scala/step-1/.gitignore
target/
.idea/
*.iml
```

```scala:instruqt-examples/scala/step-1/.scalafmt.conf
<code_block_to_apply_changes_from>
```

```scala:instruqt-examples/scala/step-1/project/Dependencies.scala
import sbt._

object Dependencies {
  lazy val scalaTest = "org.scalatest" %% "scalatest" % "3.2.0"
  lazy val phantom = "com.outworkers" %% "phantom-dsl" % "2.59.0"
  lazy val scalaReflect = "org.scala-lang" % "scala-reflect" % "2.13.6"
}
```

```scala:instruqt-examples/scala/step-1/project/build.properties
sbt.version=1.9.0
```

```scala:instruqt-examples/scala/step-1/project/plugins.sbt
addSbtPlugin("com.eed3si9n" % "sbt-assembly" % "2.1.0")
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._

class MutantsDatabase(override val connector: CassandraConnection)
    extends Database[MutantsDatabase](connector) {
  object mutants extends Mutants with Connector
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/model/Mutant.scala
package com.scylla.mms.model

case class Mutant(
    firstName: String,
    lastName: String,
    address: String,
    pictureLocation: String
)
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/service/MutantsService.scala
package com.scylla.mms.service

import com.outworkers.phantom.dsl._
import com.scylla.mms.cql.MutantsDatabase
import com.scylla.mms.model.Mutant

import scala.concurrent.Future

class MutantsService(db: MutantsDatabase) {
  import db.{session, space}

  def getAll(): Future[List[Mutant]] =
    db.mutants.select.all().fetch()

  def getByName(firstName: String, lastName: String): Future[Option[Mutant]] =
    db.mutants.select
      .where(_.firstName eqs firstName)
      .and(_.lastName eqs lastName)
      .one()

  def insertMutant(mutant: Mutant): Future[ResultSet] =
    db.mutants.store(mutant).future()

  def deleteMutantByName(
      firstName: String,
      lastName: String
  ): Future[ResultSet] =
    db.mutants
      .delete()
      .where(_.firstName.eqs(firstName))
      .and(_.lastName.eqs(lastName))
      .future()
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/Mutants.scala
package com.scylla.mms.cql

import com.outworkers.phantom.dsl._
import com.scylla.mms.model.Mutant

abstract class Mutants extends Table[Mutants, Mutant] {
  override def tableName: String = "mutant_data"

  object firstName extends StringColumn with PartitionKey {
    override def name = "first_name"
  }

  object lastName extends StringColumn with PartitionKey {
    override def name = "last_name"
  }

  object address extends StringColumn

  object pictureLocation extends StringColumn {
    override def name = "picture_location"
  }
}
```

```scala:instruqt-examples/scala/step-1/src/main/scala/com/scylla/mms/cql/MutantsDatabase.scala
package com.scylla.mms.cql
